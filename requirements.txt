langgraph>=0.2.0
langchain-core>=0.3.0
sounddevice>=0.4.6
numpy>=1.24.0
ctranslate2>=4.0.0
faster-whisper>=1.0.0
piper-tts>=1.2.0
scikit-learn>=1.3.0
scipy>=1.10.0
pydantic>=2.0.0
rich>=13.0.0

# torch/transformers were already required by src/piper_geometry's
# ResidualExtractor but were never added here (installed by hand on the
# Jetson) - listed now since src/piper_memory depends on them too.
# sentence-transformers is new: a dedicated small embedding model for
# piper_memory's retrieval, compared in parallel against reusing Qwen's
# own hidden states via ResidualExtractor.
torch>=2.0.0
transformers>=4.40.0
sentence-transformers>=3.0.0
peft>=0.10.0
